"use client";
import useRouter from "next/navigation";

export default function Home() {
  const router = useRouter();
  
  return (
    <>
      <div>
        <button onClick={() => router.push("/dsa")}>DSA</button>
        <button onClick={() => router.push("/core")}>Core</button>
      </div>
    </>
  );
}
